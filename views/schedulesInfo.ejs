<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Booking Courts App</title>
</head>
<body>
    <%- include ('partials/navbar.ejs', {showSearch: false}) %>
    <div class="container mt-3 p-2">
        <h2>Schedules</h2>
        <% for (let i=0; i < relatedSchedules.length; i++) { %>
            <div class="card p-1" style="background-color: #e0efe4;">
                <div class="card-body">
                    <h4 class="card-text">Sport Centre: <%= centre.centrename %></h4>
                    <h4 class="card-text">Sport: <%= sport.name %></h4>
                    <h5 class="card-text">Date: <%= relatedSchedules[i].date %></h5>
                    <h5 class="card-text">Available Time Slot: <%= relatedSchedules[i].time %></h5>
                    <button type="button" class="btn btn-dark">
                        <a href="/bookings/<%= centre.centreId %>/<%= sport.sportId %>/<%= relatedSchedules[i].scheId %>" style="text-decoration: none; color: white;">
                            Booking</a></button>
                </div>
            </div><br>
        <% } %>
    </div>
</body>
</html>