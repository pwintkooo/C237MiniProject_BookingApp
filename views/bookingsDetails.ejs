<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Booking Courts App</title>
</head>
<body>
    <%- include ('partials/navbar.ejs', {showSearch: false}) %>
    <div class="container mt-3 p-2">
        
        <% if (bookings.length < 1) { %>
            <h2>No Bookings Yet</h2>
        <% } else { %>
            <h2>View Bookings</h2><br>
            <% for (let i=0; i < bookings.length; i++) { %>
                <div class="card" style="background-color: #e0efe4;">
                    <div class="card-body">
                        <h3 class="card-text">Player Name: <%= bookings[i].playerName %></h3>
                        <h3 class="card-text">Email Address: <%= bookings[i].email %></h3>
                        <h3 class="card-text">Sport Centres: <%= bookings[i].location %></h3>
                        <h3 class="card-text">Sport: <%= bookings[i].sportName %></h3>
                        <h3 class="card-text">Date: <%= bookings[i].selectedDate %></h3>
                        <h3 class="card-text">Time Slot: <%= bookings[i].time %></h3>
                        <button type="button" class="btn btn-dark">
                            <a href="/updateBookings/<%= bookings[i].bookingId %>" 
                            style="color: white; text-decoration: none;" >Edit</a></button>
                        <button type="button" class="btn btn-dark"><a href="deleteBookings/<%= bookings[i].bookingId %>"
                             style="color: white; text-decoration: none;">Delete</a></button>
                    </div>
                </div><br>
            <% } %>
        <% } %>
    </div>
</body>
</html>